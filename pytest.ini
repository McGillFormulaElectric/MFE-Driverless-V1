[pytest]
# Register custom marks so pytest doesn't warn about them
markers =
    timeout: abort test after N seconds (requires pytest-timeout)
    gazebo: requires Gazebo + ROS2 environment

# Install: pip install pytest-timeout
# Usage:   pytest --timeout=300
